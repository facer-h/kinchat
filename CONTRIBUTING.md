# 贡献与协作规范

感谢参与「即时通信网络聊天室（KinChat）」项目。以下规范旨在保证多人协作时代码与文档的一致性。

## 分支策略

按功能模块拆分分支开发，`main` 作为集成主干：

| 分支 | 用途 |
|---|---|
| `main` | 集成主分支，保持可运行状态 |
| `docs` | 产品与设计文档 |
| `arch` | 架构设计 |
| `arch-serv` | 服务端模块 |
| `arch-cli` | 客户端模块 |

**规则：**

1. 不在 `main` 上直接开发，功能在对应模块分支完成后合入 `main`。
2. 分支命名用 `kebab-case`，功能分支建议 `feat/`、`fix/` 前缀。
3. 提交前先在本地核对 `git status`，避免提交无关文件。

## 提交信息规范

采用 `类型: 简述` 格式（中文或英文均可，保持一致）：

| 类型 | 说明 |
|---|---|
| `feat` | 新增功能 |
| `fix` | 修复缺陷 |
| `docs` | 文档变更 |
| `refactor` | 重构（不改变外部行为） |
| `test` | 测试相关 |
| `chore` | 构建 / 工程配置等杂项 |

示例：

```
feat: 服务端新增会话级联系人管理

fix: 修复心跳超时未清理连接的问题

docs: 补充架构设计说明
```

## 代码风格

- 语言特性：C++11 及以上。
- 命名：
  - 类型名使用大驼峰（`ChatService`、`Session`）。
  - 成员变量统一以下划线结尾（`socket_`、`buffer_`）。
  - 函数名使用驼峰（`handleMessage`、`doRead`）。
- 头文件使用 include guard（`#ifndef HEADER_XXX_H / #define / #endif`）。
- 关键类与接口补充简短中文注释，说明参数与功能。

## 提交流程

1. 检出或创建对应功能的模块分支；
2. 本地修改并自测；
3. 提交（遵循上述提交信息规范）；
4. 推送分支并入 `main`（或发起 Pull Request）。

## 文档维护

- 设计变更同步更新 [docs/](docs/) 下的对应文档。
- 需求变更维护 `docs` 分支中的 PRD 与流程图。
- 功能变更在 [CHANGELOG.md](CHANGELOG.md) 中记录。